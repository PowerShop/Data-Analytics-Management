# ระบบจัดการข้อมูลเชิงวิเคราะห์ 📊

ระบบจัดการโครงการและวิเคราะห์ข้อมูลแบบครบวงจร สำหรับการติดตามและประเมินผลโครงการต่างๆ พร้อมด้วยแดชบอร์ดการแสดงผลข้อมูลแบบไดนามิก

## 🎯 จุดประสงค์

ระบบนี้ถูกพัฒนาขึ้นเพื่อช่วยในการจัดการโครงการ ติดตามตัวชี้วัด และวิเคราะห์ข้อมูลต่างๆ ให้สามารถตัดสินใจได้อย่างมีประสิทธิภาพ โดยมีเครื่องมือการแสดงผลข้อมูลที่หลากหลายและใช้งานง่าย

## ✨ ฟีเจอร์หลัก

### 📈 การจัดการโครงการ
- **เพิ่ม/แก้ไข/ลบโครงการ** - จัดการข้อมูลโครงการอย่างครบถ้วน
- **การกรองข้อมูล** - กรองตามปี พื้นที่ ยุทธศาสตร์ หน่วยงาน
- **ระบบ CRUD แบบสมบูรณ์** - การจัดการข้อมูลที่ง่ายและรวดเร็ว
- **การจัดหมวดหมู่** - แบ่งประเภทโครงการตามยุทธศาสตร์และพื้นที่

### 📊 แดชบอร์ดและกราฟ
- **กราฟแบบไดนามิก** - สร้างกราฟได้หลากหลายรูปแบบ (แท่ง, วงกลม, เส้น, โดนัท ฯลฯ)
- **การแสดงผลแบบเรียลไทม์** - อัพเดทข้อมูลทันที
- **กราฟ 11+ แบบ** - ครอบคลุมการวิเคราะห์ทุกมิติ
- **ส่งออกรายงาน** - ส่งออกเป็น Excel และรูปแบบต่างๆ

### 💰 การติดตามงบประมาณ
- **งบประมาณหลายประเภท** - แยกตามประเภทและหมวดหมู่
- **การเปรียบเทียบ** - งบประมาณที่เสนอ vs อนุมัติ
- **การวิเคราะห์ SROI** - คำนวณผลตอบแทนทางสังคม
- **รายงานทางการเงิน** - สรุปและแนวโน้มงบประมาณ

### 🎯 การจัดการตัวชี้วัด
- **ตัวชี้วัดแบบยืดหยุ่น** - เพิ่ม/แก้ไขตัวชี้วัดได้ตามต้องการ
- **การเชื่อมโยงโครงการ** - ผูกตัวชี้วัดกับโครงการ
- **การติดตามผล** - ตรวจสอบความก้าวหน้าแบบเรียลไทม์
- **การจัดหมวดหมู่** - แยกตัวชี้วัดตามปีและประเภท

## 🛠️ เทคโนโลยีที่ใช้

### Backend
- **PHP 7.4+** - ภาษาหลักในการพัฒนา
- **MySQL** - ฐานข้อมูลหลัก
- **PDO** - การเชื่อมต่อฐานข้อมูลที่ปลอดภัย
- **RESTful API** - การสื่อสารข้อมูลแบบมาตรฐาน

### Frontend  
- **Bootstrap 5.3.2** - เฟรมเวิร์ก UI ที่ทันสมัย
- **Chart.js 4.4.1** - ไลบรารีสร้างกราฟ
- **jQuery 3.7.1** - จัดการ DOM และ AJAX
- **SweetAlert2** - กล่องข้อความที่สวยงาม
- **DataTables** - ตารางข้อมูลขั้นสูง

## 🚀 การติดตั้ง

### ความต้องการของระบบ
- PHP 7.4 หรือสูงกว่า
- MySQL 5.7 หรือสูงกว่า
- Apache/Nginx Web Server
- Composer (สำหรับ dependencies)

### ขั้นตอนการติดตั้ง

1. **Clone โปรเจค**
   ```bash
   git clone https://github.com/PowerShop/Data-Analytics-Management.git
   cd Data-Analytics-Management
   ```

2. **ติดตั้ง Dependencies**
   ```bash
   composer install
   ```

3. **ตั้งค่าฐานข้อมูล**
   - สร้างฐานข้อมูลใหม่ใน MySQL
   - นำเข้าไฟล์ `database_now.sql`
   
4. **แก้ไขการตั้งค่า**
   - แก้ไขไฟล์ `db.php` และ `db_production.php`
   - ใส่ข้อมูลการเชื่อมต่อฐานข้อมูล
   
5. **เริ่มใช้งาน**
   - เปิดเว็บไซต์ผ่าน web server
   - เข้าสู่ระบบด้วย admin account

## 📁 โครงสร้างโปรเจค

```
Data-Analytics-Management/
├── admin/                    # ส่วนผู้ดูแลระบบ
│   ├── charts/              # ระบบกราฟและแดชบอร์ด
│   ├── api/                 # API endpoints
│   └── database/            # การจัดการฐานข้อมูล
├── api/                     # API หลักของระบบ
├── vendor/                  # Composer dependencies
├── *.php                    # ไฟล์ PHP หลัก
├── composer.json           # Composer config
└── database_now.sql        # โครงสร้างฐานข้อมูล
```

## 🎨 การใช้งาน

### เข้าสู่ระบบ
1. เปิด URL ของเว็บไซต์
2. เข้าสู่ระบบด้วยบัญชี admin
3. เลือกเมนูที่ต้องการใช้งาน

### จัดการโครงการ
1. ไปที่เมนู "จัดการโครงการ"
2. คลิก "เพิ่มโครงการใหม่"
3. กรอกข้อมูลให้ครบถ้วน
4. บันทึกข้อมูล

### สร้างกราฟ
1. ไปที่เมนู "แดชบอร์ด" หรือ "กราฟ"
2. เลือกประเภทกราฟที่ต้องการ
3. ตั้งค่าตัวกรองข้อมูล
4. คลิก "สร้างกราฟ"

### ส่งออกรายงาน
1. เลือกข้อมูลที่ต้องการส่งออก
2. คลิกปุ่ม "ส่งออก Excel"
3. ดาวน์โหลดไฟล์ที่สร้างขึ้น

## 📊 กราฟที่รองรับ

- **แท่ง (Bar Chart)** - เปรียบเทียบข้อมูลหลายกลุ่ม
- **วงกลม (Pie Chart)** - แสดงสัดส่วนข้อมูล
- **เส้น (Line Chart)** - แสดงแนวโน้มตามเวลา
- **โดนัท (Doughnut Chart)** - วงกลมแบบกลวง
- **เรดาร์ (Radar Chart)** - แสดงข้อมูลหลายมิติ
- **แท่งนอน (Horizontal Bar)** - แท่งแนวนอน
- **พื้นที่ (Area Chart)** - กราฟเส้นแบบเติมสี
- **กระจาย (Scatter Plot)** - แสดงความสัมพันธ์
- **ขั้วโลก (Polar Area)** - วงกลมแบบแยกส่วน
- **แท่งผสม (Mixed Chart)** - รวมหลายประเภท
- **ปฏิทิน (Bubble Chart)** - แสดงข้อมูล 3 มิติ

## 🔧 การปรับแต่ง

### เพิ่มฟีเจอร์ใหม่
1. สร้างไฟล์ API ใหม่ใน folder `api/`
2. เพิ่มฟังก์ชันใน frontend
3. อัพเดทฐานข้อมูลถ้าจำเป็น

### แก้ไขกราฟ
1. แก้ไขไฟล์ `admin/charts/chart_builder.php`
2. เพิ่มประเภทกราฟใหม่ใน `chart_data_api.php`
3. อัพเดท JavaScript สำหรับ Chart.js

### ปรับแต่ง UI
1. แก้ไขไฟล์ CSS ใน Bootstrap
2. ปรับ layout ในไฟล์ PHP
3. เพิ่ม JavaScript สำหรับ interaction

## 🛡️ ความปลอดภัย

- **SQL Injection Protection** - ใช้ Prepared Statements
- **XSS Prevention** - ตรวจสอบ input/output
- **CSRF Protection** - ใช้ token verification
- **Session Security** - จัดการ session อย่างปลอดภัย
- **Input Validation** - ตรวจสอบข้อมูลนำเข้าทั้งหมด

## 📱 รองรับอุปกรณ์

- **Desktop** - Chrome, Firefox, Safari, Edge
- **Tablet** - iOS Safari, Android Chrome
- **Mobile** - รองรับแบบ responsive
- **ความละเอียด** - 320px ถึง 1920px+

## 🚨 การแก้ไขปัญหา

### ปัญหาที่พบบ่อย

**1. ไม่สามารถเชื่อมต่อฐานข้อมูล**
- ตรวจสอบการตั้งค่าใน `db.php`
- ตรวจสอบว่า MySQL service ทำงาน
- ตรวจสอบ username/password

**2. กราฟไม่แสดงผล**
- ตรวจสอบ console error ในเบราว์เซอร์
- ตรวจสอบว่ามีข้อมูลในฐานข้อมูลหรือไม่
- ตรวจสอบ API response

**3. ไม่สามารถส่งออก Excel**
- ตรวจสอบว่าติดตั้ง PhpSpreadsheet แล้ว
- ตรวจสอบ file permissions
- ตรวจสอบ PHP memory limit

**4. ปัญหา Performance**
- เพิ่ม database index
- ใช้ pagination สำหรับข้อมูลจำนวนมาก
- เพิ่ม caching

## 🔄 การอัพเดท

### การสำรองข้อมูล
```bash
# สำรองฐานข้อมูล
mysqldump -u username -p database_name > backup.sql

# สำรองไฟล์
cp -r Data-Analytics-Management Data-Analytics-Management-backup
```

### การอัพเดทระบบ
```bash
# Pull code ใหม่
git pull origin main

# อัพเดท dependencies
composer update

# อัพเดทฐานข้อมูล (ถ้ามี)
mysql -u username -p database_name < migration.sql
```

## 👥 การร่วมพัฒนา

หากต้องการร่วมพัฒนาระบบนี้:

1. **Fork** repository นี้
2. **สร้าง branch** ใหม่สำหรับฟีเจอร์
3. **Commit** การเปลี่ยนแปลง
4. **Push** ไปยัง branch
5. **สร้าง Pull Request**

### มาตรฐานการเขียนโค้ด
- ใช้ PSR-4 สำหรับ PHP
- Comment ให้ชัดเจน
- ตั้งชื่อตัวแปรให้สื่อความหมาย
- เขียน unit test ถ้าเป็นไปได้

## 📞 การสนับสนุน

- **Email**: support@example.com
- **GitHub Issues**: สำหรับรายงาน bug
- **Wiki**: เอกสารการใช้งานเพิ่มเติม
- **คู่มือผู้ใช้**: ในส่วน admin panel

## 📄 สัญญาอนุญาต

ระบบนี้อยู่ภายใต้สัญญาอนุญาต MIT License - ดูรายละเอียดในไฟล์ [LICENSE](LICENSE)

## 🙏 กิตติกรรมประกาศ

ขอบคุณ:
- **Chart.js** - สำหรับไลบรารีกราฟที่ยอดเยี่ยม
- **Bootstrap** - สำหรับ UI framework ที่สวยงาม
- **PhpSpreadsheet** - สำหรับการจัดการไฟล์ Excel
- **SweetAlert2** - สำหรับ modal ที่สวยงาม

## 📈 สถิติโปรเจค

- **เริ่มพัฒนา**: มกราคม 2025
- **เวอร์ชันปัจจุบัน**: 1.0.0
- **ฟีเจอร์หลัก**: 15+ ฟีเจอร์
- **ประเภทกราฟ**: 11+ แบบ
- **ตาราง Database**: 15+ ตาราง

---

## 🎉 ขอบคุณที่ใช้งาน!

หวังว่าระบบนี้จะช่วยให้การจัดการโครงการและการวิเคราะห์ข้อมูลของคุณมีประสิทธิภาพมากขึ้น 

**Happy Analyzing! 📊✨**

---

*พัฒนาด้วย ❤️ เพื่อการวิเคราะห์ข้อมูลที่ดีขึ้น*
